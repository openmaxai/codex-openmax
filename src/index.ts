// Entry point: wire Layer1 Bridge + Layer2 Adapter (local HTTP), bring up the codex app-server
// connection. Scaffold placeholder -- implemented per plan P1.
export async function main(): Promise<void> {
	throw new Error("main() not implemented (P1)");
}
