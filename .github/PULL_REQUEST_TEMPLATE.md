## What

<!-- What does this PR change? -->

## Why

<!-- Why is this change needed? Link an issue/discussion if relevant. -->

## Test plan

<!-- How was this verified? e.g. `npm test`, manual repro steps, live endpoint check. -->

- [ ] `npm test` passes locally
- [ ] `npm run build` passes locally

## Checklist

- [ ] No secrets / credentials committed
- [ ] Semgrep / Gitleaks findings addressed, or a `// nosemgrep: <rule-id>` suppression is justified inline and approved by the project lead
