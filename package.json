{
	"name": "@openmaxai/codex-openmax",
	"version": "0.1.0-alpha.0",
	"private": false,
	"description": "Codex CLI ⇆ OpenMax/CWS channel adapter (Category B): Layer 1 shared SDK bridge + Layer 2 runtime adapter.",
	"type": "module",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"typecheck": "tsc --noEmit",
		"test:types": "tsc -p tsconfig.test.json",
		"test": "npm run test:types && vitest run",
		"prepack": "npm run build"
	},
	"devDependencies": {
		"@types/node": "^22.20.1",
		"typescript": "^5.5.0",
		"vitest": "^2.0.0"
	},
	"comment": "Layer 1 will depend on @openmaxai/cws-agent-sdk once SDK v0 is published; bridge/ mocks it until then (see docs).",
	"dependencies": {
		"@openmaxai/openmax-agent-sdk": "^0.1.0-alpha.2"
	},
	"bin": {
		"codex-openmax": "dist/cli.js"
	},
	"files": [
		"dist",
		"templates",
		"docs",
		"README.md"
	],
	"publishConfig": {
		"access": "public"
	}
}