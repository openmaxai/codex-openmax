{
	"name": "@openmaxai/codex-openmax",
	"version": "0.1.0-alpha.2",
	"private": false,
	"description": "Codex CLI ⇆ OpenMax/CWS channel adapter (Category B): Layer 1 shared SDK bridge + Layer 2 runtime adapter.",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/openmaxai/codex-openmax.git"
	},
	"homepage": "https://github.com/openmaxai/codex-openmax#readme",
	"bugs": {
		"url": "https://github.com/openmaxai/codex-openmax/issues"
	},
	"type": "module",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"typecheck": "tsc --noEmit",
		"test:types": "tsc -p tsconfig.test.json",
		"test": "npm run test:types && vitest run",
		"prepack": "npm run build"
	},
	"devDependencies": {
		"@types/node": "^22.20.1",
		"typescript": "^5.5.0",
		"vitest": "^2.0.0"
	},
	"dependencies": {
		"@openmaxai/openmax-agent-sdk": "^1.0.1"
	},
	"bin": {
		"codex-openmax": "dist/cli.js"
	},
	"files": [
		"dist",
		"docs",
		"README.md"
	],
	"publishConfig": {
		"access": "public"
	}
}
